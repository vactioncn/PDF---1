# 如何提供服务器日志

## 方法1：直接复制粘贴（最简单）
1. 运行服务器：`python3 app.py`
2. 在浏览器中生成一次解读
3. 复制控制台的所有输出
4. 直接粘贴到聊天窗口发给我

## 方法2：保存到文件
运行服务器时，使用以下命令将输出保存到文件：

```bash
python3 app.py 2>&1 | tee server_log.txt
```

然后运行一次解读生成，停止服务器（Ctrl+C），把 `server_log.txt` 文件的内容发给我。

## 方法3：只保存最近的日志
如果日志太长，可以只保存最后1000行：

```bash
python3 app.py 2>&1 | tail -1000 > recent_log.txt
```

## 关键信息
请确保日志中包含以下关键信息：
- `✅ 使用 volcenginesdkarkruntime SDK` 或 `⚠️ 使用 requests 方式`
- `✅ 检测到 reasoning_content` 或 `⚠️ 未找到 reasoning_content 字段`
- `📋 message 的所有属性`
- `✅ 思考过程已添加到返回结果中` 或 `⚠️ 未检测到思考过程`


